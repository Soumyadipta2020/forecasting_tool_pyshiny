# CI requirements - minimal, pinned versions for reproducible installs and caching
# - Use this file in CI pipelines to install cached wheels quickly.
# - Keep this list intentionally small to speed builds; add more only if strictly required by CI steps.

shiny==0.5.0
pandas==1.3.0
numpy==1.20.0
matplotlib==3.4.0
seaborn==0.11.0
prophet==1.1.0
statsmodels==0.13.0
uvicorn==0.17.0

# Test/runtime tools used by CI (optional; include if your CI runs tests)
pytest==7.0.0
pytest-cov==4.0.0

# Notes:
# - If your CI includes building wheels for heavy packages (e.g., prophet), consider caching the wheel directories.
# - If you pin versions here, remember to update them when you intentionally upgrade dependencies.
